# 04-admin 本机 npm 配置
#
# legacy-peer-deps=true ：放宽 npm 7+ 的严格 peer 校验
#
# 缘起：本仓 typescript 已升到 ~6.0.2（package.json），但 openapi-typescript@7.13.0
# 的 peerDependencies 仍声明 typescript@^5.x（上游漏更）。TS 主版本极少破 runtime
# API，TS 6 实际跑 openapi-typescript codegen 完全 OK，但默认 npm install 会因
# peer 不满足直接 ERESOLVE 退出。
#
# 因此本目录钉死 legacy-peer-deps，避免每次新机 / 重装都要手挂 --legacy-peer-deps。
#
# 何时摘掉：openapi-typescript 上游 peer bump 到 ^6 之后，本文件可删。

legacy-peer-deps=true
