/**
 * ContentFilesTab is no longer used after removing the ContentGroup concept.
 * Content files are now managed directly in ExhibitContentTab.
 */
export default function ContentFilesTab() {
  return <div>此页面已弃用</div>;
}
